<% @places.each do |place| %>
  <div class="material-card col-xs-10 col-xs-offset-1">
    <h1><%= place.name %></h1>
    <i><%= place.address %></i>
    <p><%= place.description %></p>
  </div>
<% end %>
